DATABASE_URL="file:./dev.db"
SESSION_SECRET="change-me-in-production"
APP_URL="http://localhost:3000"

# Optional SMTP for invite / password-reset emails (Gmail: smtp.gmail.com, port 587, App Password)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="you@gmail.com"
# SMTP_PASS="app-password"
# MAIL_FROM="MafiaTeam <you@gmail.com>"

# Club members and the shared temp password live in prisma/club.local.json
# (gitignored). Copy prisma/club.local.example.json to start.
